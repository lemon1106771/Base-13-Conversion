# Base-13 Number Converter

This project contains two Python functions that convert numbers between decimal (base-10) and base-13.

Base-13 uses the digits:

Where:
`A = 10`
`B = 11`
`C = 12`

The converter supports:

Converting decimal to based-13 numbers

Handling both uppercase input and negative numbers

This project is intended for practicing number system conversions and working with custom numeric bases in Python.


